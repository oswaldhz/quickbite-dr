services:
  quickbitedrserver:
    image: quickbitedrserver
    build:
      context: backend/server
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 9229:9229
    command: ["node", "--inspect=0.0.0.0:9229", "src/server.js"]
